SPECIFICATION Spec

\* Define the model parameters
CONSTANTS
    Clients = {c1, c2, c3}
    MaxOperations = 5
    Fields = {f1, f2}

\* Properties to check (in order of importance)
INVARIANTS
    TypeInvariant

PROPERTIES
    StrongEventualConsistency
    OrderIndependence
    NoDataLoss
    MonotonicConvergence
    ConflictFree

\* Model checking parameters
\* MaxOperations = 5 keeps state space reasonable (~50K-100K states)
\* Increase to 10 for more thorough verification (runtime: ~5-10 minutes)
